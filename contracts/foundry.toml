[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

[rpc_endpoints]
sepolia = "${SEPOLIA_RPC_URL}"
hedera_testnet = "https://testnet.hashio.io/api"

[etherscan]
sepolia = { key = "${ETHERSCAN_API_KEY}" }
